---
import { Remarkable } from 'remarkable';

const { content } = await Astro.props;
const md = new Remarkable();
export const prerender = false;
---

<article set:html={md.render(content)} class="markdown-content" />
